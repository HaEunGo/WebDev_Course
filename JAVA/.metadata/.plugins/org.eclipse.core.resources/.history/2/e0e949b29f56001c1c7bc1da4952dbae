package com.kh.stream.intermediate;

public class B_Mapping {
	/*
	 * 매핑
	 * 	- 중간 처리 기능으로 스트림의 요소를 다른 요소로 대체하는 작업을 말한다.
	 * 		- flatMapXXX()
	 * 		- mapXXX()
	 * 		- asDoubleStream()
	 * 		- asLongStream()
	 * 		- boxed()
	 * 
	 * 
	 * 
	 */
}
